# Public site URL (used for canonical/OG/sitemap/robots)
NEXT_PUBLIC_SITE_URL=https://metis-tech.it

# SMTP settings for Contact Form (do NOT commit real secrets)
# Aruba SMTP (provided): smtps.aruba.it:465 (SSL/TLS)
SMTP_HOST=smtps.aruba.it
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=info@metis-tech.it
SMTP_PASS=YOUR_SMTP_PASSWORD

# Optional overrides
CONTACT_FROM=info@metis-tech.it
CONTACT_TO=info@metis-tech.it
